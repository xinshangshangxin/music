query get($id: ID!, $provider: Provider!, $br: BitRate) {
  get(id: $id, provider: $provider, br: $br) {
    provider
    id
    name
    lrc
    klyric
    artists {
      id
      name
    }
    album {
      name
      img
    }
    peakStartTime
    peakDuration
  }
}
