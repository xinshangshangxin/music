query search($keyword: String!, $providers: [Provider]) {
  search(keyword: $keyword, providers: $providers) {
    id
    name
    provider
    artists {
      name
    }
    album {
      name
    }
  }
}
